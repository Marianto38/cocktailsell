<div class="container  d-flex align-items-center mt-lg-4" style="position: relative;">
  <img class="h d-block d-sm-block " src="/assets/limon-4.png" style="position: absolute" alt="">
  <img class="e d-none d-sm-block " src="/assets/limon-4.png" style="position: absolute" alt="">
  <img class="f d-none d-sm-block " src="/assets/limon-4.png" style="position: absolute" alt="">
  <div class="row justify-content-center  mx-auto">
    <div class="card-body container-form">
      <h2 class="text-center">Iniciar Sesión </h2>
        <%= simple_form_for(resource, as: resource_name, url: session_path(resource_name), data: { turbo: :false }) do |f| %>
          <div class="form-inputs">
            <%= f.input :email,
                        required: false,
                        autofocus: true,
                        input_html: { autocomplete: "email" } %>
            <%= f.input :password,
                        required: false,
                        input_html: { autocomplete: "current-password" },
                        label_html: { class:"" } %>
            <%= f.input :remember_me, as: :boolean if devise_mapping.rememberable? %>
          </div>

        <div class="form-actions  d-flex justify-content-center ">
          <%= f.button :submit, "Iniciar Sesión", class: "text-decoration-nones-08 bg-primary col-12 btn-login" %>
          <% end %>
        </div>

<!--    <%= render "devise/shared/links" %> -->
        <div class="text-center mt-3 container-form-login">
          <%= link_to "Olvidó su contraseña?", new_password_path(resource_name), class: "text-decoration-none text-warning fw-bold" %>
        </div>

        <div class="mt-3 p-2">
          <p class="text-center text-dark -login fw-bold container-form-login "> Aún no tienes cuenta?
            <%= link_to "Registrate", new_registration_path(resource_name), class: "fs-md-3 text-decoration-none fw-bold text-info" %>
          </p>
        </div>

      </div>
     </div>
    </div>
  </div>
